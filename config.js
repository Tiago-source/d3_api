const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      port: 3307,
      user: "root",
      password: "",
      database: "d3",
    }
  };
  module.exports = config;